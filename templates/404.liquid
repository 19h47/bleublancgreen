{% comment %}
	404
{% endcomment %}
<div class="Sections">
	<div class="Site-container">
		<div class="row">
			<div class="col-12">
				<h1 class="text-transform-uppercase">{{ "general.404.title" | t }}</h1>
				<div class="Wysiwyg">
					<p>{{ "general.404.subtext_html" | t }}</p>
				</div>
			</div>
		</div>
	</div>
</div>
